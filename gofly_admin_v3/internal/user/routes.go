package user

import (
	"gorm.io/gorm"
)

// RegisterRoutes 注册用户和Token路由
func RegisterRoutes(db *gorm.DB) {
	// TODO: 实现用户和Token路由注册
	// 暂时注释掉以避免编译错误
	/*
		// 初始化Token服务
		tokenService := NewTokenService(db)
		tokenController := NewTokenController(tokenService)
	*/
}
