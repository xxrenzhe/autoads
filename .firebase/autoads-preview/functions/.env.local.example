# Next.js Frontend - Firebase Web SDK 配置（复制为 .env.local 后填写）

# 必填：Firebase 项目信息
NEXT_PUBLIC_FIREBASE_PROJECT_ID=gen-lang-client-0944935873
# 可选：若使用非默认 Firestore 库，填写库 ID；默认库可留空或移除此行
NEXT_PUBLIC_FIRESTORE_DB_ID=firestoredb

# 必填：Firebase Web SDK（在 Firebase 控制台 > 项目设置 > 常规 中获取）
# apiKey 一般形如 AIzaSy...
NEXT_PUBLIC_FIREBASE_API_KEY=
# authDomain 一般形如 <project-id>.firebaseapp.com 或 <project-id>.web.app
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
# storageBucket 一般形如 <project-id>.appspot.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
# messagingSenderId（数字）
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
# appId 一般形如 1:<senderId>:web:<hash>
NEXT_PUBLIC_FIREBASE_APP_ID=

# 其他前端相关（按需）
NEXT_PUBLIC_DEPLOYMENT_ENV=development
BACKEND_URL=http://127.0.0.1:8080

# 说明：
# - 将本文件复制为 .env.local，并填入实际值；Next 仅会加载 apps/frontend 目录下的 .env* 文件。
# - 若通过 docker-compose 运行，可在根目录 .env 中定义同名变量，compose 会下发到 frontend 容器。
