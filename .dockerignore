# Ignore everything by default
**

# Allow specific files required for Go builds
!go.mod
!go.sum
!go.work

# Allow the source code of all services
!services/

# Allow the frontend app, as it has its own Dockerfile and .dockerignore
!apps/frontend/

# Allow specific Dockerfiles if they exist at the root
!Dockerfile
!Dockerfile.*
