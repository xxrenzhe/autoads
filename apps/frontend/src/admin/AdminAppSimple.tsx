import React from 'react';

// 临时简化版本，避免依赖问题
export function AdminApp() {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Admin Dashboard</h1>
      <p>Admin interface temporarily simplified for build purposes.</p>
    </div>
  );
}