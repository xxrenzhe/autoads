# ClawCloud 部署配置文件

# 应用基本信息
APP_NAME=changelink-autoads
VERSION=1.0.0

# 构建配置
DOCKERFILE_PATH=Dockerfile.clawcloud
BUILD_CONTEXT=.
PORT=3000

# 健康检查（可选）
HEALTH_CHECK_PATH=/api/health
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3

# 资源配置（2C4G容器）
INSTANCE_COUNT=1
MEMORY_LIMIT=4Gi
CPU_LIMIT=2000m
NODE_OPTIONS=--max-old-space-size=3072

# 持久化存储（如果需要）
# PERSISTENT_STORAGE=true
# STORAGE_SIZE=1Gi
# STORAGE_PATH=/app/data

# 自动扩缩容（可选）
# AUTO_SCALING=true
# MIN_INSTANCES=1
# MAX_INSTANCES=5
# CPU_THRESHOLD=70
# MEMORY_THRESHOLD=80

# 日志配置
LOG_LEVEL=info
LOG_RETENTION_DAYS=7